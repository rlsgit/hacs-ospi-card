export type CardElementType = {
	type: string
	name: string
	description: string
	version?: string
}

export interface IconConfig {
	active: string
	active_disabled: string
	idle: string
	idle_disabled: string
}
